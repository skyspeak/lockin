kind = "web"
previewPath = "/"
title = "Clarity — Web"
version = "1.0.0"
id = "artifacts/clarity-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/clarity-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clarity-web", "run", "build" ]
publicDir = "artifacts/clarity-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
